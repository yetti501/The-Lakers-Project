create table PROJECT (
	name varchar(30), 
	timeStart int,
	timeEnd int, 
	timeUnits varchar(30), 
	difficulty varchar(30), 
	tools varchar(30),
	materials varchar(30),
	instructions varchar(50),
	PRIMARY KEY (name), 
)